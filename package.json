{
  "name": "what-did-you-get-done-today",
  "version": "1.0.0",
  "description": "A beautiful Gantt chart to track daily progress",
  "main": "index.html",
  "scripts": {
    "start": "live-server --port=3000 --open=index.html",
    "dev": "live-server --port=3000 --open=index.html --watch=.",
    "serve": "http-server -p 3000 -o",
    "test": "jest",
    "format": "prettier --write .",
    "build": "echo 'No build step needed for static site'",
    "deploy": "echo 'Deploy via git push to main branch'"
  },
  "keywords": [
    "productivity",
    "gantt",
    "tasks",
    "tracking"
  ],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^9.1.2",
    "http-server": "^14.1.1",
    "jest": "^29.7.0",
    "live-server": "^1.2.2",
    "prettier": "^3.5.3"
  }
}
